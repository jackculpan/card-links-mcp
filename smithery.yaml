startCommand:
  type: http
  configSchema:
    type: object
    properties:
      ref:
        type: string
        description: >
          Affiliate partner ID. When provided, card apply links in tool
          responses are replaced with your registered affiliate URLs.
          Leave blank for standard source URLs.
    required: []
  exampleConfig:
    ref: ""
